# Legacy Agent Package

The old package `orchestrator/agents/` is now isolated.

No active command or service outside this package imports it.

The active runtime is:

orchestrator/agent_runtime/

The legacy package is retained only as historical reference until final removal.
