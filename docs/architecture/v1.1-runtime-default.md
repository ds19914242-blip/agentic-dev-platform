# v1.1 Runtime Default

The default feature command now uses Agent Runtime Orchestrator.

Default:

python3 agentic.py feature "task" --product rss-agent-lab_2

Legacy:

python3 agentic.py feature-legacy

Old agent package moved to:

orchestrator/legacy_agents/agents/
